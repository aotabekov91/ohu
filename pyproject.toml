[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "ohu"
version = "0.0.1"
authors = [
    {name = "A O", email = "adhambek.otabekov.post@gmail.com"},
]
description = "A render"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
	"plug[qt] @ git+ssh://git@github.com/aotabekov/plug"
]

[project.optional-dependencies]
pdf = ["python-poppler-qt5"]
